
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders
  namespace: platform
  labels: { app: orders }
spec:
  replicas: 2
  selector: { matchLabels: { app: orders } }
  template:
    metadata: { labels: { app: orders } }
    spec:
      containers:
        - name: orders
          image: ghcr.io/OWNER/REPO/orders:latest
          ports: [{ containerPort: 8080, name: http }]
          readinessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 3 }
          livenessProbe:  { httpGet: { path: /health, port: http }, initialDelaySeconds: 10 }
---
apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: platform
spec:
  selector: { app: orders }
  ports:
    - name: http
      port: 80
      targetPort: http
