
apiVersion: apps/v1
kind: Deployment
metadata:
  name: projector-orders
  namespace: platform
  labels: { app: projector-orders }
spec:
  replicas: 2
  selector: { matchLabels: { app: projector-orders } }
  template:
    metadata: { labels: { app: projector-orders } }
    spec:
      containers:
        - name: projector-orders
          image: ghcr.io/OWNER/REPO/projector-orders:latest
          ports: [{ containerPort: 8080, name: http }]
          readinessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 3 }
          livenessProbe:  { httpGet: { path: /health, port: http }, initialDelaySeconds: 10 }
---
apiVersion: v1
kind: Service
metadata:
  name: projector-orders
  namespace: platform
spec:
  selector: { app: projector-orders }
  ports:
    - name: http
      port: 80
      targetPort: http
