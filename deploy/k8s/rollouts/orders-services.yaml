
apiVersion: v1
kind: Service
metadata: { name: orders-stable, namespace: platform }
spec:
  selector: { app: orders }
  ports: [{ name: http, port: 80, targetPort: http }]
---
apiVersion: v1
kind: Service
metadata: { name: orders-canary, namespace: platform }
spec:
  selector: { app: orders }
  ports: [{ name: http, port: 80, targetPort: http }]
